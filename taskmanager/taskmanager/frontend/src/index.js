import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';  // ← brings in App component

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
